class DASHBOARD {
	private root = '/i'

	HOME = this.root
	LOCATION = `${this.root}/location`
	SERVICES = `${this.root}/services`
	TIPS_TRICKS = `${this.root}/tips-tricks`
	TIME_BLOCKING = `${this.root}/time-blocking`
	SETTINGS = `${this.root}/settings`
}

export const DASHBOARD_PAGES = new DASHBOARD()
